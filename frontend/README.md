# Playime — Frontend

SvelteKit + Vite. See `../AGENTS.md` for the architecture and build order.

## Directory map

```
src/
  components/chat/   shared chat shell used by both Character and Story classes
  routes/character/  Character class views
  routes/story/      Story class views
```

This is a skeleton — directories are populated as their phases land.
